{% extends 'base.html.twig' %}

{% block body %}
<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 flex items-center">

{{ form_start(form, {
    action: path('search_product'),
    attr: { class: 'bg-transparent border-t border-b border-gray-300 w-[300px]'} }) }}
{{ form_widget(form) }}
{{ form_end(form) }}
</div>

{% endblock  %}
