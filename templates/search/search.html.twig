{% extends 'base.html.twig' %}

{% block body %}

{{ form_start(form, {
    action: path('search_product'),
    attr: { class: 'bg-transparent border-t border-b border-gray-300 mt-[40px]'} }) }}
{{ form_widget(form) }}
{{ form_end(form) }}

{% endblock  %}
