<nav class="navbar navbar-expand-lg ">
    <div class="container-fluid  flex align-items-center">
        <a class="navbar-brand mx-auto" href="#">
            <img src="{{ asset('logo + nom.png') }}" alt="" class="d-block mx-auto">
        </a>
        <button class="navbar-toggler text-[D66853]" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon text-[D66853]"></span>
        </button>
        <div class=" navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ">

                {% for category in categories %}
                    <li class="nav-item">
                        <a href="{{ path('app_category_show', {id: category.id}) }}" class="text-black">
                            {{ category.name }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>