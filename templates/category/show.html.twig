
<div class="flex flex-column">
    <div>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <button class="" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="">filtre</span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        {% for category in categories %}
                        <li class="nav-item">
                            <a href="{{ path('app_category_show', {id: category.id}) }}" class="text-black">
                                {{ category.name }}
                            </a>
                        </li>
                        {% endfor %}

                    </ul>
                </div>
            </div>
        </nav>
    </div>
</div>